version: 0.2
phases:
 pre_build:
   commands:
      - echo "Entered the pre_biuld phase"
 build:
   commands:
      - echo "Entered build phase"
      - mvn clean prepare-package war:exploded
 post_build:
   commands:
       - echo "Entered the post_build"
       - mv target/deploy ./
 artifacts:
    files:
      - deploy/**/*